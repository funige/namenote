'use strict'

import { View } from './view.es6'

////////////////////////////////////////////////////////////////

class PageView extends View {
  constructor(element) {
    super(element)

    this.init(element)
  }

  init(element) {
  }
}

export { PageView }
